/* 
Type script adalah javascaript yang diberi tipe.
Type script tidak bisa dijalankan di web browser karena butuh compiler 
harus di convert ke javascript.

install = npm install -g typescript
Beri Akses= set-ExecutionPolicy RemoteSigned -Scope CurrentUser  
Cek versi= tsc -v

install Compliernya = npm install -g ts-node
cek versi = ts-node -v
v10.9.1

fungsi typescrip
1.mendeteksi ada error sebelum eksekusi
2.mengetahui nilai yang akan diberikan ke suatu variabel
